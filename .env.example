# Copy to .env.local for local dev, set in Railway dashboard for production

# Port (Railway sets this automatically — don't change on Railway)
PORT=3000

# Where received files are saved locally
# SOUNDDROP_OUTPUT=/Users/yourname/Downloads/SoundDrop

# Railway Volume mount path — set this in Railway dashboard after adding a volume
# RAILWAY_VOLUME_MOUNT_PATH=/data
